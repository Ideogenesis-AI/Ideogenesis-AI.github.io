{
  "name": "ideogenesis-website",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@fontsource/inter": "^5.0.0",
    "@fontsource/jetbrains-mono": "^5.0.0"
  }
}
